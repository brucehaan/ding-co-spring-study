spring:
  application:
    name: hello-spring
  datasource:
    url: jdbc:h2:./data/demo
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console